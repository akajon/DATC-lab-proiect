package alerts

/*alert_id int IDENTITY(1, 1) primary key,
alert_owner_id int,
danger_id int,
users varchar(255),
latitude Decimal(8,6) not null,
longitude Decimal(9,6) not null,
date date not null,
foreign key (alert_owner_id) references [dbo].[users](user_id) on delete cascade,
foreign key (danger_id) references [dbo].[dangers](danger_id) on delete cascade*/
